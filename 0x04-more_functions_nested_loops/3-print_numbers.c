#include main.h
/**
 * print_numbers- print digit from 0 to 9
 *
 * Return:  Always 0.
 */
void print_numbers(void)
{
	for (int i = 48; i <= 57; i++)
	{
		putchar (i);
	}
	putchar ('\n');
	return (0);
}
