#include main.h
/**
 * mul- Multiply integer a and b and return the result.
 *@a: is an integer
 *@b: is an integer
 * Return: an integer which is a result of a * b.
 */
int mul(int a, int b)
{
	return (a * b);
	putchar ('\n');
}
